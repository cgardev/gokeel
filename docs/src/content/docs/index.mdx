---
title: gokeel
description: "Building blocks for a modular monolith in Go: a declarative transaction manager, an in-process event bus, a broker with FIFO consumers and retries over a memory or SQL engine, a transactional outbox, hierarchical log levels for log/slog, and externalized configuration."
template: splash
# The landing page omits the "Edit page" link and the prev/next pagination footer.
editUrl: false
prev: false
next: false
hero:
  title: The keel your application is built on
  tagline: Building blocks for a modular monolith in Go — declarative transactions, an in-process event bus, a broker with FIFO consumers over a memory or SQL engine, a transactional outbox, hierarchical log levels, and externalized configuration, with a standard-library core.
  image:
    file: ../../assets/keel.svg
    alt: gokeel
  actions:
    - text: Get Started
      link: /gokeel/getting-started/
      icon: right-arrow
      variant: primary
    - text: View on GitHub
      link: https://github.com/cgardev/gokeel
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components';

## Why gokeel

<CardGrid stagger>
	<Card title="Declarative transactions" icon="seti:go">
		A context-bound unit of work over `database/sql`, modeled on Spring's
		`@Transactional`: `Run` commits on success and rolls back on error or panic.
	</Card>
	<Card title="Propagation & savepoints" icon="random">
		Nested calls join the active transaction or open a savepoint according to
		the configured propagation, just like a Spring service graph.
	</Card>
	<Card title="Commit synchronizations" icon="approve-check">
		Register before-commit, after-commit, and after-completion callbacks that
		run in the same phases Spring exposes.
	</Card>
	<Card title="In-process event bus" icon="list-format">
		A small, synchronous event bus delivers each event to the listeners
		registered for its type, in registration order, with panic isolation.
	</Card>
	<Card title="Broker with two engines" icon="rocket">
		One consumer contract — FIFO queues, independent retries, dead letters —
		served in memory or durably over PostgreSQL/SQLite across nodes.
	</Card>
	<Card title="Transactional outbox" icon="seti:db">
		Events are written in the producing transaction and delivered at least
		once after commit, so a publication is never lost relative to the state
		change.
	</Card>
	<Card title="Hierarchical log levels" icon="setting">
		Spring Boot's `logging.level` model for `log/slog`: one assignment on a
		package path governs every logger beneath it, adjustable at runtime.
	</Card>
	<Card title="Externalized configuration" icon="seti:json">
		Layered JSON documents with Spring-style `${VAR:default}` placeholders,
		bound onto plain structs, with a generated JSON Schema for editors.
	</Card>
	<Card title="Zero-dependency cores" icon="seti:lock">
		The `transaction`, `eventbus`, `logging`, and `conf` modules use only the
		Go standard library, a property enforced in CI.
	</Card>
</CardGrid>
