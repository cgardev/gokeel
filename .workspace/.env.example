# Copy this file to ".env" and fill in your own values. The ".env" file is
# never committed. Its contents are loaded by docker-compose and applied as the
# git identity inside the container on startup.
GIT_USER_NAME=
GIT_USER_EMAIL=
